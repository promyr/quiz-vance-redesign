fastapi==0.140.0
uvicorn[standard]==0.35.0
gunicorn==23.0.0
sqlalchemy==2.0.43
psycopg[binary]==3.3.4
psycopg2-binary==2.9.10
pydantic==2.13.4
python-dotenv==1.1.1
# passlib==1.7.4 é incompatível com bcrypt>=4.0 (Python 3.12).
# Pinamos bcrypt==4.0.1 que é a última versão com API compatível com passlib.
# Passlib só é usado como fallback para hashes bcrypt legados ($2b$...);
# novos hashes usam PBKDF2 interno (services.py) sem dependência de passlib.
passlib==1.7.4
bcrypt==4.0.1
alembic==1.16.5
httpx==0.28.1
cryptography==48.0.1
pypdf==6.6.2
python-multipart==0.0.22
