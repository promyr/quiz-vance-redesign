workflows:
  android-release:
    name: Android Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: 3.41.4
      java: 17
      android_signing:
        - quiz_vance_android_release
    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get
      - name: Read app version
        script: |
          APP_VERSION="$(sed -nE 's/^version:[[:space:]]*([^+[:space:]]+).*/\1/p' pubspec.yaml)"
          test -n "$APP_VERSION"
          echo "APP_VERSION=$APP_VERSION" >> "$CM_ENV"
      - name: Configure Android signing
        script: |
          : "${CM_KEYSTORE_PASSWORD:?CM_KEYSTORE_PASSWORD is required}"
          : "${CM_KEY_PASSWORD:?CM_KEY_PASSWORD is required}"
          : "${CM_KEY_ALIAS:?CM_KEY_ALIAS is required}"
          : "${CM_KEYSTORE_PATH:?CM_KEYSTORE_PATH is required}"
          cat > "$CM_BUILD_DIR/android/key.properties" <<EOF
          storePassword=$CM_KEYSTORE_PASSWORD
          keyPassword=$CM_KEY_PASSWORD
          keyAlias=$CM_KEY_ALIAS
          storeFile=$CM_KEYSTORE_PATH
          EOF
      - name: Flutter analyze
        script: |
          flutter analyze --no-pub
      - name: Flutter unit tests
        script: |
          flutter test --no-pub
      - name: Build Android APK and AAB
        script: |
          flutter build apk --release --split-per-abi \
            --dart-define=APP_VERSION="$APP_VERSION"
          flutter build appbundle --release \
            --dart-define=APP_VERSION="$APP_VERSION"
    artifacts:
      - build/**/outputs/**/*.apk
      - build/**/outputs/**/*.aab
      - flutter_drive.log

  ios-release:
    name: iOS Release
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: quiz-vance-app-store-connect
    environment:
      flutter: 3.41.4
      xcode: latest
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.quizvance.quizVanceFlutter
    scripts:
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Get Flutter packages
        script: |
          flutter pub get
      - name: Read app version
        script: |
          APP_VERSION="$(sed -nE 's/^version:[[:space:]]*([^+[:space:]]+).*/\1/p' pubspec.yaml)"
          test -n "$APP_VERSION"
          echo "APP_VERSION=$APP_VERSION" >> "$CM_ENV"
      - name: Install pods
        script: |
          find . -name "Podfile" -execdir pod install \;
      - name: Flutter analyze
        script: |
          flutter analyze --no-pub
      - name: Flutter unit tests
        script: |
          flutter test --no-pub
      - name: Build iOS IPA
        script: |
          flutter build ipa --release \
            --build-number=$PROJECT_BUILD_NUMBER \
            --dart-define=APP_VERSION="$APP_VERSION" \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log
